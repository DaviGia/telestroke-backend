version = '1.1.0'

ext {
  mainVerticleName = 'it.unibo.telestroke.registry.RegistryVerticle'
}

dependencies {
  implementation project(':common')

  implementation "io.vertx:vertx-redis-client:$vertxVersion"
}
